version: "2"
run:
  timeout: 5m
  modules-download-mode: "readonly"

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
    - goimports
    - misspell
    - unconvert
    - unparam
    - gocyclo
    - gofmt
    - revive